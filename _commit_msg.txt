chore: v2.9.0 发布前清理 — 死代码 / 过时命名 / rapidocr 兼容残留（-940 行）

两个维度审查（死代码 + 命名/兼容残留）后清理，pytest 30 项 + 全量
import + CLI 冒烟通过：

死代码删除：
- 孤儿 tools（无法运行/被取代）：bench_trt_build（import 已坏）、
  eval_viterbi、tune_params、_bench_padding（sweep 已是 no-op）、
  check_deps（跟踪 rapidocr 时代依赖）
- config：OCR_REC_BATCH_NUM、SPEED_UNITS、COMPAT_CONF_*、
  ERROR_DETECT_CANDIDATE_THRESHOLD
- constants：Flag.CONFIRMED_SEG、Flag.is_anchor（+ 对应测试）
- ocr_engine：_sg_coeff_cache、8 个无消费者 re-export、空节标记
- ocr_text：extract_speed_value 的 rapidocr tuple 分支（44 行）、
  convert_speed_to_kmh
- video_utils：codec_from_fourcc、safe_int/safe_float（+ 测试）、
  冗余 hashlib import
- error_detection：ErrorReport 5 个死字段 + candidate 构建循环 +
  split_results 参数（联动 pipeline 调用）
- viterbi：path_values 死数据流（签名 + 写入）
- correction：_generate_candidates confidence_score 参数、
  _fill_unrecoverable conservative 参数、reocr_only=False 死分支
  （联动 pipeline/gui_export 调用链）、compute_confidence 兼容 API
  （联动 gui.py 死分支）、hasattr(crop_bgr,'data') 防御分支
- pipeline：_MEM_DIAG 门控代码（保留 _rss_mb/_sum_nbytes，gui 在用）、
  _correct 的 corrections 死参数、冗余 sys import
- headless：--progress 死分支；RaceVideoToLog.py：--video-backend
  死选项；csv_io：video_backend 死字段；gpu_setup：cuda 后端死链
- gui：get_ocr_engine、_codec_cache、first_frame_rgb、QPainter/QPen/
  QColor 模块级 import；gui_review：_speed_label、_get_correction_xy、
  review_scope 参数；gui_analysis：delta_label 参数、QSpinBox import

过时命名/注释：
- pyproject：移除 pyclipper/shapely（rapidocr 遗留）、py-modules 补
  ocr_native（wheel 构建缺失）、rapidocr TRT 注释更新
- spec paddle 注释、tensorrt.py shim docstring、signals.py
  "（向后兼容）"、ocr_engine 头部"旧模块 re-export"框架
- DEPENDENCIES.md 全文重写为 v2.9.0

Co-Authored-By: Claude <noreply@anthropic.com>

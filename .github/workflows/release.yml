# RaceVideoToLog 发布工作流 — 从 master 最新提交一键构建并发布 GitHub Release
#
# 手动触发（Actions → Release → Run workflow，默认 ref: master）即完成：
#   1. 校验版本引用一致性（tools/version.py，退出码 1 = 不一致即中止）
#   2. 读取 config.__version__（单一事实源），确认 tag v<版本> 不存在
#   3. 下载 decord fork 发布产物 decord-<ver>-win64-gpu.zip（chr431/decord，
#      decord-version 输入，默认 0.7.11）解压到 _decord_build\
#   4. setup_venv.bat --ci + build_exe.bat --ci 构建 EXE（--ci 跳过 pause）
#   5. 打包 RaceVideoToLog.<版本>.7z（7-Zip LZMA2 mx=9 最大压缩，win-latest
#      runner 预装 7z）—— 布局与现有 release 一致（RaceVideoToLog.exe 在
#      7z 根 + _internal/，无文件夹包裹；.7z 比 .zip 小 ~26%，Win11 原生解压）
#   6. 打 tag v<版本> + push，创建 GitHub Release（notes 取自 release_notes.md 对应节）
#
# 版本号不通过输入指定 —— 始终发布当前 ref 的 config.__version__（tools/version.py
# 已强制所有引用一致 + release_notes.md 有对应节）。发布流程：dev 上 bump → 填
# release_notes → 合并到 master → 运行本 workflow。
#
# 只监听 workflow_dispatch（push/tag 不会二次触发）。构建失败时不会产生任何
# commit / tag / release（tag 在构建成功后才创建）。

name: Release

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Branch to release from'
        required: false
        default: 'master'
        type: string
      decord-version:
        description: 'decord fork release version to consume (chr431/decord)'
        required: false
        default: '0.7.11'
        type: string

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0  # 全历史：release notes / tag 检查需要
          token: ${{ github.token }}
          submodules: recursive  # 引擎子模块 third_party/video_ocr_engine（spec 打包其模型资产）

      - name: Export engine submodule path (PYTHONPATH)
        shell: pwsh
        run: |
          # setup_venv 尚未运行（.pth 未写入）时，早期步骤（如 import config →
          # from engine_config import *）需能把引擎子模块源码根放上 sys.path
          $engine = Join-Path (Get-Location) 'third_party\video_ocr_engine'
          Write-Host "engine path: $engine"
          echo "PYTHONPATH=$engine" >> $env:GITHUB_ENV

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate version references
        shell: pwsh
        run: |
          python tools/version.py
          if ($LASTEXITCODE -ne 0) {
            Write-Error "版本引用不一致（可能缺 release_notes.md 对应节）。先在 dev 上: python tools/version.py bump X.Y.Z"
            exit 1
          }

      - name: Read version + check tag
        id: ver
        shell: pwsh
        run: |
          $ver = (python -c "import config; print(config.__version__)").Trim()
          if ($LASTEXITCODE -ne 0 -or -not $ver) {
            Write-Error "读取 config.__version__ 失败"
            exit 1
          }
          Write-Host "Releasing version: $ver"
          if (git tag -l "v$ver" | Select-String -Quiet "v$ver") {
            Write-Error "tag v$ver 已存在。如需重新发布先删 tag，或先在 dev 升版本后合并到 master"
            exit 1
          }
          Add-Content -Path $env:GITHUB_OUTPUT -Value "version=$ver"
          Add-Content -Path $env:GITHUB_OUTPUT -Value "tag=v$ver"

      - name: Download decord fork release
        shell: pwsh
        run: |
          $ver = "${{ inputs.decord-version }}"
          $url = "https://github.com/chr431/decord/releases/download/v$ver/decord-$ver-win64-gpu.zip"
          Write-Host "Downloading $url"
          curl.exe -L -f -o decord.zip $url
          if ($LASTEXITCODE -ne 0) {
            Write-Error "decord v$ver 发布产物下载失败。请先运行 chr431/decord 的 Release workflow 发布 decord-$ver-win64-gpu.zip"
            exit 1
          }
          Expand-Archive -Path decord.zip -DestinationPath .
          if (-not (Test-Path "_decord_build/decord.dll")) {
            Write-Error "_decord_build/decord.dll 缺失（zip 布局不符）"
            exit 1
          }
          if (-not (Test-Path "_decord_build/python/decord/video_reader.py")) {
            Write-Error "decord Python 层缺失（next_roi/get_codec 不可用）"
            exit 1
          }
          Write-Host "decord $ver extracted to _decord_build/"

      - name: Setup venv + install deps
        shell: pwsh
        run: |
          cmd /c "setup_venv.bat --ci"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build EXE
        shell: pwsh
        run: |
          cmd /c "build_exe.bat --ci"
          if ($LASTEXITCODE -ne 0) {
            Write-Error "EXE 构建失败"
            exit 1
          }
          if (-not (Test-Path "dist/RaceVideoToLog/RaceVideoToLog.exe")) {
            Write-Error "dist/RaceVideoToLog/RaceVideoToLog.exe 未生成"
            exit 1
          }

      - name: Smoke check EXE (headless, CPU backend, no GPU needed)
        # 构建成功 ≠ 产物可用：本步骤在打 tag/发布**之前**真实运行
        # frozen exe（合成视频 + CPU 解码 + ONNX OCR），把 import 面/
        # 打包遗漏类回归（如 cuda-python 13.3.1 merged-wheel 连锁崩）
        # 拦在发布前。失败不产生 tag/release。
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "_smoke" | Out-Null
          ffmpeg -y -f lavfi -i "testsrc=duration=3:size=640x360:rate=30" `
            -pix_fmt yuv420p "_smoke\input.mp4" 2>$null
          if (-not (Test-Path "_smoke\input.mp4")) {
            Write-Error "ffmpeg 合成测试视频失败"
            exit 1
          }
          $ver = "${{ steps.ver.outputs.version }}"
          dist\RaceVideoToLog\RaceVideoToLog.exe "_smoke\input.mp4" `
            --roi 20 20 300 80 --decode-backend cpu --ocr-backend cpu `
            --no-monitor --frame-end 60 -o "_smoke\out.csv"
          if ($LASTEXITCODE -ne 0) {
            Write-Error "frozen exe 冒烟运行失败（exit $LASTEXITCODE）"
            exit 1
          }
          $csv = Get-Content "_smoke\out.csv" -Encoding UTF8
          if (-not ($csv | Where-Object { $_ -match "RaceVideoToLog v$ver" })) {
            Write-Error "CSV 头缺少 'RaceVideoToLog v$ver'"
            exit 1
          }
          $n = ($csv | Where-Object { $_ -notmatch "^#" -and $_.Trim() }).Count
          Write-Host "smoke OK: CSV $n 行（读数可为空——冒烟只验产物可运行）"

      - name: Check artifact size (<400MB)
        shell: pwsh
        run: |
          $size = (Get-ChildItem "dist/RaceVideoToLog" -Recurse | `
                   Measure-Object -Property Length -Sum).Sum
          $mb = [math]::Round($size / 1MB)
          Write-Host "dist 总体积: ${mb}MB"
          if ($size -gt 400MB) {
            Write-Error "产物总体积 ${mb}MB 超过 400MB 上限"
            exit 1
          }

      - name: Package release archive (7z, max compression)
        shell: pwsh
        run: |
          $ver = "${{ steps.ver.outputs.version }}"
          $sz = "C:\Program Files\7-Zip\7z.exe"
          if (-not (Test-Path $sz)) {
            Write-Error "7-Zip 未找到（windows-latest runner 预装，见 https://github.com/actions/runner-images）"
            exit 1
          }
          # 在 dist/RaceVideoToLog 内打包：* 使 RaceVideoToLog.exe 位于 7z 根
          # 参数：-mx=9 最大压缩；-m0=LZMA2 默认强熵编码；-mmt=on 多线程
          Push-Location "dist/RaceVideoToLog"
          try {
            & $sz a -t7z -mx=9 -m0=LZMA2 -mmt=on "..\..\RaceVideoToLog.$ver.7z" "*"
            if ($LASTEXITCODE -ne 0) { throw "7z 打包失败 (exit $LASTEXITCODE)" }
          } finally { Pop-Location }
          # 校验布局与现有 release 一致：exe 在根 + _internal/ 子目录
          $sl = & $sz l -slt "RaceVideoToLog.$ver.7z"
          $paths = $sl | Where-Object { $_ -match "^Path = " } | `
                   ForEach-Object { ($_ -replace "^Path = ", "").Trim() }
          $hasRootExe = $paths -contains "RaceVideoToLog.exe"
          $hasInternal = [bool]($paths | Where-Object {
              $_ -like "_internal\*" -or $_ -like "_internal/*" })
          if (-not $hasRootExe -or -not $hasInternal) {
            Write-Error "7z 布局不符：需 RaceVideoToLog.exe（根）+ _internal/（与现有 release 一致）"
            exit 1
          }
          Write-Host "archive ready: RaceVideoToLog.$ver.7z"

      - name: Create tag + push
        shell: pwsh
        run: |
          $ver = "${{ steps.ver.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "v$ver"
          git push origin "v$ver"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Write-Host "tag v$ver pushed"

      - name: Generate release notes
        id: notes
        shell: pwsh
        run: |
          $ver = "${{ steps.ver.outputs.version }}"
          $escaped = $ver -replace '\.', '\.'
          $notes = [System.IO.File]::ReadAllText((Resolve-Path "release_notes.md"))
          $m = [regex]::Match($notes, "(?ms)^## v$escaped(.*?)(?=^## v\d|\z)")
          if (-not $m.Success) {
            Write-Error "release_notes.md 中未找到 v$ver 节"
            exit 1
          }
          $body = ("## v$ver" + $m.Groups[1].Value).TrimEnd()
          if ($body -match "### 待补充") {
            Write-Error "release_notes.md 的 v$ver 节还是骨架（### 待补充）。请先在 dev 填写后重新合并到 master"
            exit 1
          }
          $sha = (git rev-parse --short HEAD).Trim()
          $body += "`n`n---`n构建源: ${{ inputs.ref }} @ $sha（GitHub Actions 自动构建，工具链 decord ${{ inputs.decord-version }}）"
          [System.IO.File]::WriteAllText("notes.md", $body, (New-Object System.Text.UTF8Encoding($false)))

      - name: Create GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${{ steps.ver.outputs.version }}" "RaceVideoToLog.${{ steps.ver.outputs.version }}.7z" `
            --title "v${{ steps.ver.outputs.version }}" `
            --notes-file notes.md
